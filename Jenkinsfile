pipeline {pipeline    agent any

    stages {
        stage('Webhook Test') {
            steps {
                sh 'echo "✅ Jenkins triggered automatically from GitHub push"'
            }
        }
    }
}
``
